// OwnerNavbar — Thin wrapper re-exporting the dashboard Navbar for the owner layout
import DashboardNavbar from "../../components/dashboard/Navbar";

const Navbar = () => <DashboardNavbar />;

export default Navbar;
